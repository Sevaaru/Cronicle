<?xml version="1.0" encoding="utf-8"?>
<!-- Tarjeta individual para el StackView del widget 2x2.
     Cada carta muestra la portada en pantalla completa con superposición
     de título, progreso, badge de tipo y botón +1.
     Los fill-in intents los pone LibraryWidgetService. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_library_background">

    <!-- Portada a pantalla completa. -->
    <ImageView
        android:id="@+id/widget_card_poster"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:background="@drawable/widget_poster_placeholder"
        android:contentDescription="@string/widget_library_title" />

    <!-- Degradado inferior para legibilidad del título. -->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:layout_gravity="bottom"
        android:scaleType="fitXY"
        android:src="@drawable/widget_small_scrim" />

    <!-- Insignia tipo (esquina superior izquierda). -->
    <TextView
        android:id="@+id/widget_card_kind"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="8dp"
        android:background="@drawable/widget_kind_badge_background"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingTop="2dp"
        android:paddingBottom="2dp"
        android:textColor="@color/widget_on_primary"
        android:textSize="10sp"
        android:textStyle="bold"
        android:text="—" />

    <!-- Título y progreso (esquina inferior izquierda). -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:paddingStart="10dp"
        android:paddingEnd="52dp"
        android:paddingBottom="10dp">

        <TextView
            android:id="@+id/widget_card_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFFFF"
            android:textSize="13sp"
            android:textStyle="bold"
            android:maxLines="2"
            android:ellipsize="end"
            android:shadowColor="#80000000"
            android:shadowDx="0"
            android:shadowDy="1"
            android:shadowRadius="3"
            android:text="—" />

        <TextView
            android:id="@+id/widget_card_progress"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="#E6FFFFFF"
            android:textSize="11sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:shadowColor="#80000000"
            android:shadowDx="0"
            android:shadowDy="1"
            android:shadowRadius="2"
            android:text="—" />
    </LinearLayout>

    <!-- Botón +1 (esquina inferior derecha). -->
    <ImageView
        android:id="@+id/widget_card_plus"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_gravity="bottom|end"
        android:layout_margin="8dp"
        android:background="@drawable/widget_plus_background"
        android:src="@drawable/widget_ic_plus"
        android:scaleType="center"
        android:contentDescription="@string/widget_library_increment" />

</FrameLayout>
