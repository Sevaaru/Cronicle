<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <application
        android:label="cronicle"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:enableOnBackInvokedCallback="true"
        tools:replace="android:allowBackup,android:fullBackupContent">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Trakt OAuth: deep link al proceso principal (navegador externo + app_links).
                 No usar CallbackActivity del plugin: Custom Tab no abre bien cronicle:// en Android 15+. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="cronicle" android:host="trakt-oauth"/>
                <data android:scheme="cronicle" android:host="anilist-oauth"/>
                <data android:scheme="cronicle" android:host="steam-oauth"/>
            </intent-filter>
        </activity>
        <!-- flutter_web_auth_2: solo Twitch (HTTPS→puente). Trakt va a MainActivity. -->
        <activity
            android:name="com.linusu.flutter_web_auth_2.CallbackActivity"
            android:exported="true"
            android:taskAffinity="">
            <intent-filter android:label="flutter_web_auth_2">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="cronicle" android:host="twitch-oauth"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Wear OS companion bridge.
             Listens for messages from the watch app (`/library/request_sync` and
             `/library/action`) and reads / writes the local Drift SQLite directly,
             so it works even when the Flutter UI is not running.
             See android/app/src/main/kotlin/.../wear/ for implementation. -->
        <service
            android:name="com.cronicle.app.cronicle.wear.WearLibraryListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/library" />
            </intent-filter>
        </service>

        <!-- Foreground service that briefly boots a headless FlutterEngine to push
             watch-originated changes to AniList / Trakt. -->
        <service
            android:name="com.cronicle.app.cronicle.wear.WearRemoteSyncService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Cronicle Library home-screen widget (Material 3, resizable) -->
        <receiver
            android:name="com.cronicle.app.cronicle.widget.LibraryWidgetProvider"
            android:exported="true"
            android:label="@string/widget_library_label_medium">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.cronicle.app.cronicle.widget.ACTION_REFRESH" />
                <action android:name="com.cronicle.app.cronicle.widget.ACTION_DATA_CHANGED" />
                <action android:name="com.cronicle.app.cronicle.widget.ACTION_TEMPLATE" />
                <action android:name="com.cronicle.app.cronicle.WEAR_LIBRARY_CHANGED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_library_info" />
        </receiver>

        <receiver
            android:name="com.cronicle.app.cronicle.widget.LibraryWidgetProviderSmall"
            android:exported="true"
            android:label="@string/widget_library_label_small">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.cronicle.app.cronicle.widget.ACTION_REFRESH" />
                <action android:name="com.cronicle.app.cronicle.widget.ACTION_DATA_CHANGED" />
                <action android:name="com.cronicle.app.cronicle.widget.ACTION_TEMPLATE" />
                <action android:name="com.cronicle.app.cronicle.WEAR_LIBRARY_CHANGED" />
                <action android:name="com.cronicle.app.cronicle.widget.ACTION_PREV_SMALL" />
                <action android:name="com.cronicle.app.cronicle.widget.ACTION_NEXT_SMALL" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_library_info_small" />
        </receiver>

        <receiver
            android:name="com.cronicle.app.cronicle.widget.LibraryWidgetProviderLarge"
            android:exported="true"
            android:label="@string/widget_library_label_large">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.cronicle.app.cronicle.widget.ACTION_REFRESH" />
                <action android:name="com.cronicle.app.cronicle.widget.ACTION_DATA_CHANGED" />
                <action android:name="com.cronicle.app.cronicle.widget.ACTION_TEMPLATE" />
                <action android:name="com.cronicle.app.cronicle.WEAR_LIBRARY_CHANGED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_library_info_large" />
        </receiver>

        <service
            android:name="com.cronicle.app.cronicle.widget.LibraryWidgetService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Google Sign-In / Play services: visibilidad de paquete (Android 11+) -->
        <package android:name="com.google.android.gms" />
    </queries>
</manifest>
